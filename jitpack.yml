# configuration file for building snapshots and releases with jitpack.io
jdk:
  - openjdk17
before_install:
  - chmod +x gradlew
install:
  - ./gradlew :achalasecure:assembleRelease
  - ./gradlew :achalasecure:publishToMavenLocal
